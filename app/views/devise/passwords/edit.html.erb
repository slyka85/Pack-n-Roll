<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8">
    <title>Edit Password</title>
  </head>
  <body>
    <h2>Change your password</h2>
    <%= simple_form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :put }, html: {class: "form-signin"}) do |f| %>
    <%= f.error_notification %>
    <%= f.input :reset_password_token, as: :hidden, input_html: {class: "form-control"} %>
    <%= f.full_error :reset_password_token %>
    <%= f.input :password, label: "New password", required: true, input_html: {class: "form-control"}, autofocus: true, hint: ("#{@minimum_password_length} characters minimum" if @minimum_password_length) %>
    <%= f.input :password_confirmation, label: "Confirm your new password", required: true, input_html: {class: "form-control"} %>
    <%= f.button :submit, "Change my password", class: "btn btn-primary btn-block" %>
    <% end %>
    <%= render "devise/shared/links" %>
  </body>
</html>