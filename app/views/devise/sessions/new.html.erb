<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8">
    <title>Log In</title>
  </head>
  <body>
<!--     <style>
      body {
      background-image: url("http://www.tochkali.com/sky2.gif");
      background-size: cover;
      }
    </style> -->
    <%= simple_form_for(resource, as: resource_name, url: session_path(resource_name), html: {class: "form-signin middle"}) do |f| %>
    <div class="space" height="500px"></div>
    <h2 class="text-center">Log in</h2>
    <%= f.input :email, required: false, autofocus: true, label: false, placeholder: "Email", input_html: {class: "form-control"} %>
    <%= f.input :password, required: false, label: false, placeholder: "Password", input_html: {class: "form-control"} %>
    <%= f.button :submit, "Log in", class: "btn btn-primary btn-block login" %>
    <% end %>
    <%= render "devise/shared/links" %>
  </body>
</html>